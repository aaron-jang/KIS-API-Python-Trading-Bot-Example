services:
  trading-bot:
    build: .
    container_name: kis-trading-bot
    restart: always
    env_file:
      - .env
    environment:
      - TZ=Asia/Seoul
    volumes:
      # 프로젝트 디렉토리에 직접 적재
      - ./data:/app/data
      - ./logs:/app/logs
